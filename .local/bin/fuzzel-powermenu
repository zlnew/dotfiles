#!/usr/bin/env bash

ACTION=$(printf "Cancel\nPoweroff\nReboot\nSuspend\nHibernate" | \
fuzzel --dmenu -p "⚡ Power:" --lines=5 --width=30)

case "$ACTION" in
  "Poweroff") systemctl poweroff ;;
  "Reboot") systemctl reboot ;;
  "Suspend") systemctl suspend ;;
  "Hibernate") systemctl hibernate ;;
  *) exit 0 ;;
esac

