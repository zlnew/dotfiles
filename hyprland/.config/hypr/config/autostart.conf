# Autostart wiki https://wiki.hyprland.org/Configuring/Keywords/#executing

source = ~/.config/hypr/config/defaults.conf

# System / environment (core foundation)
exec-once = systemctl --user import-environment
exec-once = hash dbus-update-activation-environment 2>/dev/null
exec-once = dbus-update-activation-environment --systemd
exec-once = wl-paste --watch cliphist store

# Input
exec-once = fcitx5 -d

# Security / policy
exec-once = /usr/lib/polkit-kde-authentication-agent-1 &

# Idle / power / monitor management
exec-once = ~/.config/hypr/scripts/monitor-hook.sh
exec-once = $idlehandler

# Core UI processes
exec-once = waybar &
exec-once = mako &

# Background utilities
exec-once = bash -c "mkfifo /tmp/$HYPRLAND_INSTANCE_SIGNATURE.wob && tail -f /tmp/$HYPRLAND_INSTANCE_SIGNATURE.wob | wob & disown"

# Cosmetic
exec-once = nm-applet --indicator
exec-once = ~/.config/hypr/scripts/set-wallpaper.sh

