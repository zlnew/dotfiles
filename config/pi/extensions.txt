# pi extensions, one source per line (`#` comments, blank lines ignored).
# Installed idempotently by provision.sh (`pi list` check, then `pi install`).
npm:pi-mcp-adapter
npm:pi-subagents
npm:pi-web-access
npm:@juicesharp/rpiv-ask-user-question
npm:@juicesharp/rpiv-todo
npm:billion-context
npm:@amaster.ai/pi-memory-mem0
npm:@ff-labs/pi-fff
