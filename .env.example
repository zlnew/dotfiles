# Global machine secrets. Copy to ~/.env and fill in (never commit ~/.env).
# Format: KEY=VALUE (no spaces, no quotes needed). # lines are comments.
MEM0_API_KEY=
OPENCODE_ZEN_API_KEY=
OPENCODE_GO_API_KEY=
GOOGLE_API_KEY=
OLLAMA_API_KEY=
